<Window x:Class="Filebuloso.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Height="520" Width="520" MinHeight="520" MinWidth="520">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Default directory" VerticalAlignment="Center" Margin="0,0,12,0"/>
        <TextBox x:Name="DefaultDirectoryTextBox"
                 Grid.Column="1"
                 MinWidth="240"
                 Margin="0,0,0,12"/>

        <CheckBox x:Name="ConfirmCheckBox"
                  Grid.Row="1"
                  Grid.ColumnSpan="2"
                  Content="Confirm before processing"
                  Margin="0,0,0,12"/>

        <CheckBox x:Name="DryRunCheckBox"
                  Grid.Row="2"
                  Grid.ColumnSpan="2"
                  Content="Enable dry run"
                  Margin="0,0,0,12"/>

        <Border Grid.Row="4" Grid.ColumnSpan="2" BorderThickness="0" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <StackPanel Margin="0,0,0,8">
                    <Image Source="pack://application:,,,/Resources/logo.png"
                           Width="64"
                           Height="64"
                           Stretch="Uniform"
                           HorizontalAlignment="Left"/>
                    <TextBlock Text="Filebuloso" FontSize="18" FontWeight="SemiBold" Margin="0,6,0,0"/>
                </StackPanel>
                <TextBlock Text="Version 0.1b" Foreground="#555555"/>
                <TextBlock Text="Copyright © 2026" Foreground="#555555"/>
                <TextBlock Text="Moses Ling" Foreground="#555555" Margin="0,0,0,8"/>
                <TextBlock Text="Development Credit:" FontWeight="Bold"/>
                <TextBlock Text="Coding agent: Codex &amp; Claude" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="This software is released under the MIT License" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button Content="View License" Padding="10,4" Click="ViewLicenseButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="90" Margin="0,0,12,0" Click="SaveButton_Click"/>
            <Button Content="Reset" Width="90" Margin="0,0,12,0" Click="ResetButton_Click"/>
            <Button Content="Cancel" Width="90" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
