<Window x:Class="Filebuloso.Views.AddEditCategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Category" Height="320" Width="420" MinHeight="320" MinWidth="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name" VerticalAlignment="Center" Margin="0,0,12,0"/>
        <TextBox x:Name="NameTextBox" Grid.Column="1" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="Extensions" VerticalAlignment="Top" Margin="0,0,12,0"/>
        <TextBox x:Name="ExtensionsTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Height="140"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2"
                   Grid.ColumnSpan="2"
                   Text="Enter extensions separated by commas or new lines."
                   Foreground="Gray"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="90" Margin="0,0,12,0" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="90" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
